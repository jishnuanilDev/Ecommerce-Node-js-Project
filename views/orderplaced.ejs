<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Thank You Page</title>
  <!-- Add Bootstrap CSS link -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Custom CSS to center the container */
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    /* Optional: Add a background color or image */
    body {
      background-color: #f8f9fa; /* Replace with your desired color */
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="jumbotron text-center">
      <h1 class="display-4">Thank You for Shopping!</h1>
      <p class="lead">Your order has been placed successfully.</p>
      <hr class="my-4">
      <p>Your order details:</p>

      <a class="text-primary" id="download" href="">Click here to download here invoice</a>
      <!-- Display order details here, e.g., order number, items, total amount, etc. -->
      <a class="btn btn-primary btn-lg" href="/user/userhomepage" role="button">Continue Shopping</a>
    </div>
  </div>

  <!-- Add Bootstrap JS and Popper.js scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    var downloadLink = document.getElementById('download');
    downloadLink.addEventListener('click',async function() {
      let userData = 'ihijdiodidoidoodo';
try{

fetch('/user/invoiceDownload',{
  method:"GET"
}).then(response=>{
  response.json()
}).then(data=>{
  console.log(data)
})


}catch(err){
console.error('error fetching data');
res.status(500).send('Error fetching data')
}

      const text = 'hello, how are you boy, this is just sample text';
     
      const blob = new Blob([text], { type: 'text/plain' });
  
      downloadLink.href = window.URL.createObjectURL(blob);
      downloadLink.click();
    });
  </script>
</body>
</html>
